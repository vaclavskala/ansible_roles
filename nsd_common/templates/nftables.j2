{% for ip in nsd_ipv4_address %}
ip daddr {{ ip }} meta l4proto {tcp, udp} th dport 53 accept comment "allow dns for {{ ip }}"
{% endfor %}

{% for ip in nsd_ipv6_address %}
ip6 daddr {{ ip }} meta l4proto {tcp, udp} th dport 53 accept comment "allow dns for {{ ip }}"
{% endfor %}
